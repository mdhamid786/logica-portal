export const metadata = {
  title: 'Next.js',
  description: 'Reset Password',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
